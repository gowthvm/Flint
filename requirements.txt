# Runtime dependencies, pinned for reproducible builds and CI runs.
# Bump deliberately and re-run the full gate suite (ruff/mypy/pytest and
# the release regression script) when updating.
PyQt6==6.11.0
psutil==7.2.2
pywin32==312
wmi==1.5.1

# Optional, build only: `pip install wheel setuptools` to compile the native
# writer extension (core/_native_writer.c, see setup.py). Without it, writes
# fall back to the pure-Python buffered path automatically.